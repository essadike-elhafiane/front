/**
 * @type {import('next').NextConfig}
 */

const nextConfig = {
    images: {
        unoptimized: true,
      },
    // output: 'export',
    // images: { unoptimized: true } 
}

export default nextConfig
